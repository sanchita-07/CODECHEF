#include <stdio.h>

int main()
{
    int t;
    scanf("%d",&t);
    while(t--){
        int a[4];
        scanf("%d",&a[4]);
    /*  sum = a+b;
        div = a/b;
        mul = a*b;
        dif = a-b;
        for(int i=0; i<n; i++){
            if(a[i]==sum)
            w++;
            if(a[i]==dif)
            x++;
            if(a[i]==mul)
            y++;
            if(a[i]==div)
            z++;

        }
        if(w==1 && x==1 && y==1 && z==1)
        printf("%d %d",a,b);
        else
        printf("-1 -1");
    */
        
    }
    return 0;
}
#include<bits/stdc++.h>

#define ll long long
#define PB push_back
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while(t--)
    {
        ll n;
        cin>>n;
        ll a[n];
        for(ll i=0; i<n; i++){
            cin>>a[i];
        }
        map<ll,ll>mp;
        vector<ll>v;
        ll x = 1;
        ll i;
        while(x>0 && x<INT_MAX){
            v.PB(x);
            x*=k;

        }
    }
    return 0;
}